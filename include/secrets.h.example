#pragma once
// =====================================================================
//  secrets.h.example - plantilla. Copiar como "secrets.h" y completar
//  con tus datos reales; secrets.h esta en .gitignore y no se sube.
// =====================================================================
struct WifiCred { const char* ssid; const char* pass; };
static const WifiCred WIFI_LIST[] = {
  {"TU_WIFI_1", "TU_CLAVE_1"},
  {"TU_WIFI_2", "TU_CLAVE_2"},
};
static const size_t WIFI_COUNT = sizeof(WIFI_LIST) / sizeof(WIFI_LIST[0]);

static const char* MQTT_USER = "tu-usuario-mqtt";
static const char* MQTT_PASS = "tu-clave-mqtt";
